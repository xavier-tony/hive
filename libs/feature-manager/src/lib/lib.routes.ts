import { Route } from '@angular/router';
import { FeatureManagerComponent } from './feature-manager/feature-manager.component';

export const featureManagerRoutes: Route[] = [
  { path: '', component: FeatureManagerComponent },
];
